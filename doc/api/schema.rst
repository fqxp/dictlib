Defining a schema
=================
To be able to use features like validation or complex conversion, you need a
schema to describe the contents and types of your dictionaries. To make runtime
schema definition easy, we will almost always work with instances of the class
`Schema`. There are two ways to define a schema 

.. autoclass:: dictlib.schema.Schema(DictField)
   :members:
   
